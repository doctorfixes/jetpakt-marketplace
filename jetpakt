#!/usr/bin/env bash
# JetPakt CLI shim. Usage: ./jetpakt <command> [options]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
exec python3 -m jetpakt_cli.cli "$@"
