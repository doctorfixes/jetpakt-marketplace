# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Secrets — NEVER commit
.env
.env.*
*.pem
*.key
secrets/
credentials.json

# Local runtime artifacts (keep structure, skip mutable outputs)
output/pulse/drafts/
output/pulse/pulse_runs.jsonl

# Large generated PDFs can stay tracked; snapshots stay tracked too (they're the audit trail)

# Stripe live catalog dumps (contain account_id; safe but prefer not in public-ish clones)
# data/stripe_catalog.json  # kept in repo for now — no secrets
